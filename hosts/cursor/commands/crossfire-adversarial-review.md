# Crossfire adversarial review

Run a read-only adversarial (design/risk challenge) review and return the output verbatim. REVIEW-ONLY.

```bash
crossfire adversarial-review --self cursor "$ARGUMENTS"
```
