# Crosscheck review

Run a read-only cross-agent review of the current changes and return the output verbatim. REVIEW-ONLY: do not modify files or turn findings into fixes.

```bash
crosscheck review --self cursor "$ARGUMENTS"
```
