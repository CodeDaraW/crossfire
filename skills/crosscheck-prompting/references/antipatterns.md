# Antipatterns

- Embedding routing flags (`--background`, `--write`) inside the task text.
- Asking a read-only rescue to "go ahead and fix it" (use `--write` instead).
- Solving the task yourself in the host instead of delegating.
- Vague requests with no success criteria.
